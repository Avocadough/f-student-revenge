# ผลตรวจเดโม 0.1

ตรวจวันที่ 23 กันยายน 2026 ด้วย Godot 4.7.2 Stable บน Windows ผลนี้แยกการทดสอบระบบ การเล่นอัตโนมัติ การตรวจเว็บ และความรู้สึกของผู้เล่นออกจากกัน

## ผลที่ตรวจแล้ว

| ขอบเขต | ผล / ข้อจำกัด |
| --- | --- |
| นำเข้าและส่งออก Web | ผ่าน ไม่มี parse error; Compatibility renderer, single-thread |
| ระบบด่าน/บอส | 61 assertions ผ่าน รวมประตู checkpoint จำนวนศัตรู คอมโบซ้ำของ AI โล่ Web และแกน F; มีการจัด fixture และเรียก API เพื่อแยกพฤติกรรม |
| การเล่นผ่าน controller จริง | 28 assertions ผ่าน รวมคอมโบ J/K, hit window, การ์ด, parry/counter, dodge, pause, แพ้และลองใหม่ |
| เดินเกมอัตโนมัติ | ผ่านทั้ง 9 ห้อง/3 ด่านถึงตอนจบ ใน 140.55 วินาทีจำลอง ไม่แก้ HP, ไม่อมตะ, ไม่ teleport และไม่เรียกโจมตีศัตรูโดยตรงในการเดินเกม; bot อ่านสถานะศัตรูได้ จึงไม่แทนผู้เล่นทั่วไป |
| เมนูและบันทึก | 25 assertions ผ่าน รวม Esc ในหลายสถานะ เริ่มใหม่ เล่นต่อ และไฟล์บันทึกผิดรูปแบบ ใช้ user directory สำหรับทดสอบแยกต่างหาก |
| ทางสำรองการควบคุมเว็บ | 15 assertions ผ่าน; ปฏิเสธการจับเมาส์แล้วไม่ติดพัก, จับสำเร็จแล้วหลุดจึงพัก, ลากปุ่มกลางหมุนกล้องได้; actual captured-camera branch ตรวจไม่ได้ใน headless |
| Asset | 18 GLB มี `.blend` คู่กัน; ไม่มี texture/buffer ภายนอก; character skin และคลิปตรวจด้วย Blender render จริง |
| เสียง | โหลดครบ 12 เสียง; pool จำกัด 8 เสียงหลังยิงคำขอ 400 ครั้ง; pause หยุดเสียงต่อสู้และยังใช้เสียง UI ได้ |
| เว็บ localhost | ตรวจภาพจริงใน Codex In-app Browser: เมนู วิธีเล่น ตั้งค่า บันทึกค่ากล้องแล้วรีโหลด เลือกด่าน เริ่มเกม ท่าโจมตี แพ้ ลองใหม่ HP เต็ม และพักเกม |
| GitHub Pages สาธารณะ | Deployment สำเร็จ; HTML/wasm HTTP 200; PCK ดาวน์โหลดกลับ hash ตรงกับไฟล์ส่งออก; เปิดหน้าเมนู → บทนำ → ด่านแรก → กดโจมตี → ถูกศัตรูทำความเสียหาย → พักเกมได้จริงใน IAB |

แก้ข้อบกพร่องที่พบจากการตรวจ: บอสถูกโจมตีหนักล็อกจนไม่สวนกลับ, Esc ทำให้หน้าจบด่านติดค้าง, เริ่มเกมใหม่เก็บสถานะจบเดิม, กลับเมนูแล้วเล่นต่อทันทีลบ node ซ้ำ และ JSON เสียแสดง error แม้ fallback ได้แล้ว

Codex In-app Browser ปฏิเสธ pointer lock (`WrongDocumentError`) จึงเพิ่มทางสำรองให้เล่นคีย์บอร์ดและลากปุ่มเมาส์กลางหมุนกล้องได้ การออกจากการจับเมาส์ที่เคยสำเร็จยังพักเกมตามปกติ ส่วน Chrome/Edge แบบแอปแยกยังไม่ได้ทดสอบในการรันครั้งนี้

## ประสิทธิภาพที่วัดจริง

Native Godot, GL Compatibility, NVIDIA RTX 5060 Ti, viewport ที่ระบบอนุญาตจริง **1920×1061**, ห้องที่ 2 ของด่าน AI, ศัตรู 4 ตัว, warm-up 2 วินาทีแล้วเก็บข้อมูล 12.00 วินาที: **179.97 FPS เฉลี่ย**, p95 5.988 ms, p99 6.173 ms และไม่มีเฟรมพักเกม

การวัดนี้ใช้ผู้เล่นยืนนิ่งและอมติเพื่อคง workload ของศัตรู ไม่ใช่การทดสอบผ่านเกม ไม่ใช่ผลของเว็บ และไม่ยืนยันเครื่องสเปกต่ำหรือจอ 1080p เต็มขนาด

## วิธีตรวจซ้ำ

จาก root ของโครงการ โดย `godot` ชี้ไปที่ Godot 4.7.2:

```text
godot --headless --path . --editor --import --quit
godot --headless --path . --script tools/test_stage_subsystem.gd
godot --headless --path . --fixed-fps 60 --script tools/test_gameplay.gd
godot --headless --path . --script tools/test_menu_persistence.gd
godot --headless --path . --script tools/test_browser_fallback.gd
godot --path . --resolution 1920x1080 --script tools/measure_performance.gd
godot --headless --path . --export-release Web docs/index.html
```

สร้างโฟลเดอร์ `evidence/` ก่อนรันทดสอบ ผล JSON ที่ตรวจแล้วเก็บสำเนาใน `Verification/`; log และภาพชั่วคราวเก็บใน `evidence/` ซึ่งไม่เผยแพร่ทั้งหมด

## สิ่งที่ยังไม่ยืนยัน

ยังไม่มีการเล่นด้วยคนต่อเนื่องครบทุกด่านบน Chrome/Edge, ไม่มีการวัดความสนุกกับกลุ่มผู้เล่นใหม่, ไม่มีผล benchmark เว็บหรือเครื่องสเปกต่ำ, ไม่ได้ทดสอบ mobile และไม่ได้ยืนยันความยาว 15–20 นาที การทดสอบที่ผ่านบอกว่าระบบเดินถึงตอนจบได้ภายใต้เงื่อนไขที่ระบุ ไม่ใช่ข้อยืนยันว่าเกมสมดุลและสมบูรณ์ทุกกรณี
